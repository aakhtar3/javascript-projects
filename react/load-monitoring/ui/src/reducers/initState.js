/**
 * Default App state.
 */
export const appState = {
  isReady: false,
};

/**
 * Default Event state.
 */
export const eventState = {
  currentTime: new Date(),
  events: [],
  hasAlertThreshHold: false,
};
